{
  "checks": {
    "{{name}}": {
      "handlers": ["{{'", "'.join(handlers)}}"],
      "command": "{{command}}",
      "interval": {{interval}},
      "occurrences": {{occurrences}},
{% if standalone %}
      "standalone": true
{% else %}
      "subscribers": ["{{'", "'.join(subscribers)}}"]
{% endif %}
    }
  }
}
